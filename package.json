{
  "name": "bioshield-agent",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "cd backend && set PYTHONUTF8=1&& .venv\\Scripts\\python.exe -m uvicorn main:app --port 8001",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:all": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
